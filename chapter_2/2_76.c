/*
 * 2.76
 * Suppose we are given the task of generating code to multiply integer variable `x`
 * by various different constant factoes `K`. To be efficient, we want to use only the
 * operations +, -, and <<. For the following values of `K`, write C expressions to
 * perorm the multiplication using at most three operations per expression.
 * 
 * A. K = 17:
 * B. K = -7:
 * C. K = 60:
 * D. K = -112:
 */

// A. (x << 4) + x
// B. x - (x << 3)
// C. (x << 6) - (x << 2)
// D. (x << 4) - (x << 7)

int main (int argc, char **argv){
    return 0;
}
